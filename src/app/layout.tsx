import type { ReactNode } from "react";
export const metadata={title:"Berry Voice",description:"TES AI customer operations agent"};
export default function RootLayout({children}:{children:ReactNode}){return <html lang="en-ZA"><body>{children}</body></html>;}
